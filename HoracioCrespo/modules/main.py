from circle_area import calculate_area
from circle_perimeter import calculate_perimeter

radius = input('enter the radius: ')

print(calculate_area(float(radius)))
print(calculate_perimeter(float(radius)))
